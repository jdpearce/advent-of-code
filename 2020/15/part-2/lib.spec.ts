import { calculateNumberOnTurn } from '../part-1/lib';

describe('2020-12-15.2', () => {
  const input = ``;

  it('should calculate high turn numbers', () => {
    expect(calculateNumberOnTurn([0, 3, 6], 30000000)).toBe(175594);
  });
});
